# 변경 이력

이 문서는 CodexManager의 외부에 보이는 변경 사항을 기록하는 버전 이력의 단일 기준 문서입니다.
형식은 Keep a Changelog를 참고하되 현재 저장소 운영 방식에 맞게 가볍게 조정했습니다.

## [Unreleased]

## [0.5.0] - 2026-07-23

### Added

- `chatgpt.com` Device Code 계정 인증을 추가했습니다. 코드 표시/복사, 인증 페이지 열기, 자동 polling, 세션 취소와 만료 처리를 지원하며 브라우저 인증 및 콜백 URL 수동 처리는 그대로 사용할 수 있습니다(#370).
- `/skills/`를 **Skills 설치**와 **Codex 플러그인 설치** 탭으로 분리했습니다. 내장/사용자 지정 GitHub 저장소, skills.sh 검색 결과, ZIP 및 기존 디렉터리에서 Skill을 개별 설치하고 저장소를 백그라운드에서 갱신하거나 안전하게 제거할 수 있습니다. Codex 기본 Marketplace의 표준 플러그인 전체 설치는 유지되며 `.system` Skill은 읽기 전용입니다(#126).
- `gpt-image-2`(스냅샷 `gpt-image-2-2026-04-21`)를 아홉 번째 builtin이자 일반 목록에 노출되는 여덟 번째 모델로 추가했습니다. 이 항목은 `/v1/images/generations`와 `/v1/images/edits`에서만 사용되며 text, Chat Completions 또는 직접 Responses 메인 모델로는 노출되지 않습니다. 카탈로그 가격 메타데이터에는 1M image input / cached input / output token당 공식 8 / 2 / 30 USD 요금을 저장합니다.

### Changed

- 릴리스 버전을 `0.5.0`으로 올리고 workspace, frontend package, Tauri desktop metadata 및 lock file을 동기화했습니다.
- Frontend lint error와 warning을 정리해 릴리스 전 품질 검사가 깔끔하게 통과하도록 했습니다.

### Fixed

- Hybrid rotation에서 aggregate API route만 설정된 model이 local account pool로 먼저 전송되던 문제를 수정했습니다. Aggregate-only model은 이제 binding된 aggregate API로 직접 전송되고, dual route는 account 우선·aggregate fallback을 유지하며, account-only model은 binding되지 않은 aggregate API를 사용하지 않습니다(#381).
- Windows tray preview window에 최신 크기가 안정적으로 적용되지 않던 문제를 수정했습니다. 새로 만들거나 재사용하는 window에 고정 `360 × 430` logical size와 constraint를 다시 적용해 content clipping과 이전 window 크기 잔존을 방지합니다(#380).
- Upstream이 `response.created`를 먼저 보낸 뒤 nested `response.failed` event로 quota 소진을 알릴 때 Responses WebSocket이 자동으로 계정을 전환하지 못하던 문제를 수정했습니다. 이제 content가 없는 lifecycle event를 임시 저장하고 nested error를 인식하며, 소진된 계정을 즉시 limited로 표시한 뒤 다음 사용 가능한 계정에서 현재 요청을 조용히 다시 실행합니다.
- Aggregate API에 `compatible` 통합 호환 provider type을 추가했습니다. 하나의 upstream URL과 key로 중복 provider record 없이 입력 요청 경로를 그대로 사용하여 Codex/OpenAI와 native Claude 요청을 모두 처리할 수 있습니다(#359).
- ChatGPT `/api/auth/session`에서 `accessToken`만 가져온 계정이 Codex upstream 요청에서 401을 반환하던 문제를 수정했습니다. HTTP, Responses WebSocket 및 계정 warmup 경로는 필요할 때 누락된 AgentIdentity를 등록·저장하고, 계정 binding을 검증한 뒤 AgentAssertion을 사용하며, 공식 nested snake_case/camelCase AgentIdentity 형식을 모두 지원합니다(#376).
- `/api/auth/session` 계정이 일반적인 가져온 계정 placeholder로 표시되던 문제를 수정했습니다. 사용자가 수동으로 편집한 이름은 유지하며, access token의 OpenAI profile 또는 session user 정보에서 표시 이름을 가져오고 기존 placeholder 라벨을 업그레이드합니다.
- OpenAI 공식 API 가격에 맞춰 GPT-5.6 Sol, Terra, Luna 과금을 수정했습니다. Cached input에 90% 할인을 적용하고 272K input 임계값부터 input 2배, output 1.5배의 long-context 요금으로 전환합니다. 기존 자동 추정 가격은 공식 가격으로 마이그레이션하며 custom 가격은 유지합니다.

## [0.4.4] - 2026-07-20

### Changed

- 릴리스 버전을 `0.4.4`로 올리고 workspace, frontend package, Tauri desktop metadata 및 lock file을 동기화했습니다.
- `release-all` workflow가 `v*` tag push로도 publishing을 시작할 수 있도록 추가했습니다.

### Fixed

- 계정 풀의 “usage refresh”가 선택한 계정에 token이 없거나 refresh 가능한 대상이 없을 때도 성공으로 표시되던 문제를 수정했습니다. Service는 구조화된 실행 결과를 반환하고 frontend는 skipped, partial, successful refresh를 구분하며 account 및 usage cache를 갱신합니다.
- Background task 설정에서 더 짧은 interval을 저장하거나 task를 끈 뒤에도 기존 sleep cycle이 끝날 때까지 기다리던 문제를 수정했습니다. Usage polling, gateway keepalive, token refresh polling은 대기 중에도 동적 설정을 다시 읽습니다.

## [0.4.3] - 2026-07-19

### Added

- 계정별 proxy 관리 기능을 추가했습니다(#314). 재사용 가능한 proxy profile을 만들고 개별 계정에 지정하여 계정 요청, token refresh 및 usage refresh 경로에 적용할 수 있습니다.
- Desktop 및 Web service mode에서 proxy 연결, latency, download/upload를 테스트하고 runtime 상태, egress IP, 국가/지역 및 속도 결과를 통합 표시합니다.
- 모델 카탈로그에 다중 선택 일괄 라우트 할당을 추가했습니다. 계정 풀 또는 Aggregate API 라우트를 여러 모델에 추가/업데이트하거나 기존 라우트를 모두 교체할 수 있으며 upstream 모델 이름은 각 모델 slug에서 자동으로 채워집니다.

### Changed

- AIXiamo 후원 문구를 간결하게 줄이고 해외 결제 카드가 없는 중국 사용자를 위한 ChatGPT, Claude, Codex 충전 및 사후 지원 정보를 강조했습니다.
- Codex gateway 판별을 강화하고 현재 provider gateway URL을 우선 사용합니다. 일반 OpenAI-compatible endpoint의 오판을 방지하면서 compact, Gemini 및 다중 후보 fallback 동작은 유지합니다(#346).
- Native Web Search, Image Generation, `/v1/models`, Responses WebSocket의 protocol 호환성을 개선하고 zstd encoded content를 크기 제한과 함께 decode합니다(#363).
- 릴리스 버전을 `0.4.3`으로 올리고 workspace, frontend package, Tauri desktop metadata 및 lock file을 동기화했습니다.

### Fixed

- 모델 관리 페이지에서 관리자가 라우트 일괄 할당 진입점을 항상 볼 수 있도록 했습니다. 모델을 선택하기 전에는 명확한 안내와 함께 비활성화되어 기능을 찾기 어려운 문제를 해결합니다.
- 일부 Responses 400 이후 후보 failover가 중단되던 문제를 수정했습니다. 재시도 시 적용할 수 없는 session/thread header를 제거하고 encrypted content container를 올바르게 보존합니다.
- Hosted image tool과 Codex image-generation namespace의 충돌을 해결하고 legacy Responses path suppression 범위를 의도한 경로로 제한했습니다.
- Windows tray context menu가 즉시 닫히거나 click 시 refresh로 회귀하던 문제와 usage update 후 menu label이 갱신되지 않던 문제를 수정했습니다(#362).
- Windows glass effect에서 header badge text가 흐리게 보이던 문제(#360)와 sidebar logo 및 선택된 navigation icon의 흰색 fill 문제(#361)를 수정했습니다.
- 모델 카탈로그의 지표, 필터, 표 헤더, 소스, 가격/지시/라우트 상태 및 내장 모델 설명을 중국어 UI에서 영어와 섞이지 않도록 완전히 현지화했습니다.
- Release compilation, frontend lint/effect, 모호한 end-to-end selector 및 Service 병렬 test isolation 문제를 수정하여 전체 release validation을 복구했습니다.

## [0.4.2] - 2026-07-19

### Changed

- 관리자 대시보드의 사용량 지표 카드에 오늘의 전체 사용량, 캐시 입력 및 추론 출력 사용량을 함께 표시합니다.
- 릴리스 버전을 `0.4.2`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

### Fixed

- 지운 요청 로그가 다시 나타나던 문제를 수정했습니다. 과금된 항목은 운영 로그 조회에서 숨기되 불변 사용량 및 과금 감사 데이터는 보존합니다.
- Aggregate API Responses 연결 probe가 `input_text` 콘텐츠를 보내도록 수정하고, upstream 오류 세부 정보를 보존하여 UI에서 테스트 실패 내용을 확인할 수 있게 했습니다.
- `codexmanager-web`이 `/v1/responses` WebSocket 핸드셰이크를 일반 GET 요청으로 다운그레이드하여 405를 반환하던 문제를 수정했습니다. 이제 Web gateway는 인증 및 Codex 헤더를 보존하면서 WebSocket 프레임을 양방향 전달하며, 관리형/온보딩 provider 설정도 Responses WebSocket을 명시적으로 활성화합니다.
- README의 원격 Star History 차트 임베드를 저장소 내 이미지로 교체하여 그래프가 계속 표시되도록 했습니다.

## [0.4.1] - 2026-07-14

### Changed

- 모델 카탈로그를 V2로 일괄 전환했습니다. 새 DB에는 builtin 모델 8개가 seed되며 일반 목록에는 7개가 표시되고, 모델·정수 가격 구간·route·권한 그룹·instructions 정책이 하나의 트랜잭션으로 저장됩니다.
- Gateway, 로컬 `/v1/models`, 모델 그룹 및 선택기는 V2만 읽습니다. 원격 merge/prune, 공급자 `/models` 탐색, 요청 시 bootstrap 및 컴파일 가격 fallback을 제거했습니다.
- 로컬 JSON preview/commit과 가격·route·instructions 편집을 추가했습니다. builtin은 비활성화만 가능하고 custom 모델은 삭제할 수 있습니다.
- `models_cache.json`은 데스크톱/Web에서 사용자가 명시적으로 내보낼 때만 생성되며 모델 personality prompt를 포함하지 않습니다.
- Codex 캐시 내보내기에 GPT-5.6 Max/Ultra와 `multi_agent_version`, `tool_mode`, Responses Lite, 최대 컨텍스트, comp hash 등 런타임 메타데이터를 보존합니다. 명시적인 Ultra 요청은 클라이언트 Ultra와 업스트림 Max로 각각 기록됩니다.
- 지갑 과금은 정수 가격 구간, 불변 charge snapshot 및 멱등 ledger 업데이트를 하나의 트랜잭션으로 기록합니다.
- GPT-5.6 Sol, Terra, Luna의 기본 input/output 가격을 각각 1M tokens당 5/30, 2.5/15, 1/6 USD로 추가했습니다. Cached input은 보수적으로 input과 같은 가격을 적용하며, 마이그레이션은 아직 가격이 없고 편집되지 않은 builtin만 채웁니다.
- 릴리스 버전을 `0.4.1`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

### Fixed

- 이전 V2 builtin이 밑줄 ID를 사용할 때 SQLite 외래 키 오류 787로 데스크톱 시작이 거부되던 문제를 수정했습니다.
- 기존 모델을 열 때 모델 카탈로그 편집기에 빈 초안이 표시되던 문제를 수정하고 중국어 UI의 필드명, 컨트롤 제목 및 접근성 이름을 보완했습니다.
- 데스크톱 세션 초기화 중 관리자 딥 링크가 홈 화면으로 재설정되던 문제를 수정했습니다.
- 후보 업스트림별로 Codex 요청 재작성을 격리하여 요청 본문 변경이 후보 간에 섞이지 않도록 했습니다.

## [0.4.0] - 2026-06-24

### Changed
- Codex 첫 연결 안내를 단순화해 `auth.json`과 `config.toml`의 최소 필수 설정만 남겼습니다.
- 릴리스 버전을 `0.4.0`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

## [0.3.9] - 2026-06-11

### Added
- 상태별 계정 정리에 Unknown 상태 삭제 옵션을 추가했습니다.
- 비활성화된 Aggregate API도 발견된 모델 풀과 모델 매핑을 보존하여 다시 활성화할 때 재사용할 수 있게 했습니다.

### Fixed
- 새로 가져온 계정이 사용량 새로고침 상태 처리에서 unavailable 상태로 덮이지 않던 경로를 보완했습니다.
- 동일 세션의 `prompt_cache_key`가 바뀌지 않도록 고정했습니다.
- 세션 종료 후 refresh token 무효화 정리 경로를 보완했습니다.
- `CODEXMANAGER_CODEX_IMAGE_GENERATION_AUTO_INJECT_TOOL` 기본값을 `0`으로 변경하여 일반 Responses 요청에는 기본적으로 `image_generation` tool을 자동 주입하지 않습니다.

### Changed
- 업스트림 relay 호환성 관측 정보를 강화했습니다.
- 릴리스 버전을 `0.3.9`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

## [0.3.8] - 2026-05-30

### Fixed
- proxy가 `x-codex-turn-state`만 보존하고 요청에 `prompt_cache_key`가 남아 있을 때 local prompt-cache account binding을 재사용하지 못하던 문제를 수정하고 `anchor_mode` 진단 로그를 추가했습니다.
- `/v1/messages/count_tokens` 요청이 실제 Responses 생성으로 잘못 전달될 수 있던 문제를 local token estimation으로 수정했습니다.
- Anthropic-native 및 Claude Code compatibility 경로의 Cloudflare challenge 대응을 강화했습니다. compatibility stream의 `zstd`를 비활성화하고, challenge cooldown 및 candidate switching을 추가하며, low-quota account는 tail fallback으로만 사용합니다.
- Claude Code 2.x mid-conversation `system` message를 Responses `developer` role로 정규화해 ChatGPT Codex backend의 Cloudflare challenge 가능성을 낮췄습니다.

### Changed
- 릴리스 버전을 `0.3.8`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

## [0.3.7] - 2026-05-27

### Added
- `release-all.yml`에 Linux arm64 게시 대상을 추가해 통합 multi-platform release pipeline에 포함했습니다.

### Fixed
- OpenAI 호환 `/v1/responses`에서 `prompt_cache_key`를 사용할 때 local account routing이 불안정하던 문제를 수정했습니다. 동일한 client key는 같은 upstream account를 재사용하고, upstream 요청에는 원래 client key를 그대로 보존합니다.
- `pc_1` 같은 짧은 `prompt_cache_key`가 local account binding에서 제외되어 balanced rotation으로 분산되던 문제를 수정했습니다.
- ChatGPT upstream transport 오류가 후보 account failover를 계속 타지 못하던 문제를 수정했습니다.
- Responses WebSocket upstream handshake에서 beta marker header가 빠지던 문제를 수정했습니다.
- `/v1/messages` compatibility stream의 terminal `message_stop` 인식이 일관되지 않던 문제를 수정했습니다.
- account upsert 시 기존 token이 지워질 수 있던 문제를 수정했습니다.

### Changed
- 릴리스 버전을 `0.3.7`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

## [0.3.6] - 2026-05-25

### Added
- 예약된 account warmup 기능을 추가해 설정된 account를 주기적으로 예열하고 첫 요청의 cold-start 실패 가능성을 낮췄습니다.
- 실험적인 ChatGPT `/v1/responses` WebSocket upstream transport를 추가했습니다. `CODEXMANAGER_USE_WEBSOCKET_UPSTREAM=1` 설정 시 gateway가 WebSocket을 먼저 시도하고 실패하면 HTTP streaming으로 fallback하며, upstream proxy, connect timeout, protocol frame 검증 동작을 유지합니다.

### Changed
- request log의 key 열이 ID 대신 API key 이름을 보여주도록 변경해 호출 출처를 더 쉽게 추적할 수 있게 했습니다.
- 릴리스 버전을 `0.3.6`으로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

## [0.3.5] - 2026-05-24

### Added
- Settings에 `/v1/responses/compact` 요청에만 적용되는 compact 모델 전달 규칙을 추가했습니다.
- 고동시성 스트리밍 지연을 추적할 수 있도록 request gate 대기, 첫 응답 지연, 느린 trace stdout 진단을 추가했습니다.

### Fixed
- tool call 이후 Anthropic SSE completed snapshot이 다시 재생되어 클라이언트에 assistant 텍스트가 중복 표시되던 문제를 수정했습니다.
- Responses가 reasoning summary만 반환하는 Chat Completions 호환 경로를 수정해 streaming 및 non-streaming 응답에 호환 reasoning 필드를 노출합니다.
- region-blocked 계정은 자동 token refresh를 중단해 백그라운드 polling이 같은 실패를 반복하지 않도록 했습니다.
- model sync 이후 오래된 Aggregate API/model source routes와 orphan automatic catalog models를 정리합니다.
- `CODEXMANAGER_REQUEST_GATE_WAIT_TIMEOUT_MS=5000`으로 request gate 대기 상한을 opt-in 설정할 수 있게 해 동일 key/path/model 스트리밍 요청의 긴 head-of-line blocking을 줄입니다.

### Changed
- 릴리스 버전을 `0.3.5`로 올리고 workspace, frontend package, Tauri desktop metadata, lock file을 함께 동기화했습니다.

### Added
- Aggregate API 잔액 검사가 이제 계정 풀 사용량 polling과 같은 주기로 자동 새로고침되며, Settings의 usage polling worker 설정을 따릅니다.
- Codex 이미지 생성 호환 경로를 추가했습니다. `/v1/responses`는 기본적으로 공식 Codex 동작에 맞춰 `image_generation` tool을 자동 주입하고, 명시적으로 전달된 tool은 그대로 전달하며, `/v1/images/generations` 및 `/v1/images/edits` 호환 엔드포인트를 제공합니다. 기본 이미지 tool 모델은 `gpt-image-2`입니다.
- Codex CLI 첫 연결 안내에 `auth.json` 설정 단계를 추가해 플랫폼 Key, `auth.json`, `config.toml`의 관계를 명확히 했습니다.

### Fixed
- Spark 전용 쿼터가 표시되지 않던 문제를 수정했습니다. 추가 쿼터는 `additional_rate_limits[].rate_limit`에서 계속 파싱됩니다.
- 사용량 상세 모달을 조정해 추가 쿼터가 많을 때 두 열과 스크롤로 확인할 수 있게 했습니다.

### Changed
- 릴리스 버전을 `0.3.2`로 올리고 workspace, 프런트엔드 패키지, Tauri 데스크톱, lock file을 함께 동기화했습니다.
- 릴리스 버전을 `0.2.6`으로 올리고 workspace, 프런트엔드 패키지, Tauri 데스크톱, lock file을 함께 동기화했습니다.
- README 진입 문서에서 최근 커밋 블록을 제거해 안정적인 기능 설명과 문서 링크만 남겼습니다.
- Settings의 gateway transport 카드에 upstream 전체 타임아웃 설정을 복원했습니다. `CODEXMANAGER_UPSTREAM_TOTAL_TIMEOUT_MS`를 여기서 확인하고 수정할 수 있으며, 기본값 `0`은 service가 전체 요청 시간으로 끊지 않는다는 뜻입니다.
- 이미지 업로드, 큰 `b64_json` 응답, 오래 걸리는 이미지 생성을 위해 Nginx 예시 설정에 `/v1/images/` 전용 proxy 블록을 추가했습니다.
- 요청 로그 비용 추정을 공식 `gpt-5.5`, `gpt-5.5-pro`, `gpt-image-*` 가격에 맞췄습니다. 현재 usage 로그에는 modality 분리가 없으므로 이미지 모델은 공식 Image token 가격으로 보수 추정합니다.

## [0.1.19] - 2026-04-08

### Added
- Aggregate API가 이제 여러 인증 유형과 사용자 지정 `action` 설정을 지원하며, passthrough 라우팅도 인증 방식과 동작에 따라 올바른 upstream을 선택합니다.
- i18n에 언어 선택 유지와 분리된 메시지 카탈로그를 추가하고, 대시보드·모달·사이드바·사용량 라벨의 남은 다국어 문구를 모두 보강했습니다.

### Fixed
- Aggregate API passthrough 스트림에서 Anthropic `message_stop` 이벤트를 올바르게 인식하도록 수정해 스트림 조기 종료와 상태 오판 가능성을 줄였습니다.
- 게이트웨이가 지원하지 않는 `service_tier` 값을 upstream으로 계속 전달하던 문제를 수정했습니다. 이제 표준 Responses 요청은 지원되는 값만 유지해 upstream 거절을 피합니다.
- 협업 문서와 보안 문서의 루트 진입 페이지를 기본 중국어 본문으로 바꾸고, 다국어 문서 진입 흐름도 함께 정리해 저장소 루트에서 바로 열기 쉬워졌습니다.

### Changed
- 공식 문서를 언어별 폴더 기준으로 재정리했고, 루트 진입 문서와 다국어 랜딩 문구도 함께 정비했습니다.
- 릴리스 버전을 `0.1.19`로 올리고 workspace, 프런트엔드 패키지, Tauri 데스크톱, lock file, README, CHANGELOG의 버전 설명을 함께 갱신했습니다.

## [0.1.18] - 2026-04-06

### Added
- 계정 목록에 직접 정렬 컨트롤과 잔여 쿼터 우선 정렬을 추가해 쿼터가 빠듯한 계정을 더 빨리 찾을 수 있게 했습니다.
- Gemini CLI 기본 호환 계층을 추가해 streaming `tools`, MCP 도구 이름, SSE `tool_call` 같은 핵심 요청 경로를 보강했습니다.

### Fixed
- 계정 페이지의 “쿼터 상세” 팝업 위치가 어긋나던 문제를 수정해 오른쪽 패널이 쿼터 개요 카드의 중앙선에 맞춰지도록 했습니다.
- completed tool output이 일반 본문으로 처리되던 문제, 스트리밍 cached-token 로그 수집, 요청 적응 호환성, token refresh 경계 조건 등 Gemini 관련 문제를 수정했습니다.

### Changed
- Gemini → Codex / Responses 요청 경로를 CPA 호환 방향으로 정렬해 developer message, 도구 이름 매핑, FIFO `call_id`, `reasoning`, `include`, `parallel_tool_calls`를 올바르게 보강하도록 했습니다.
- 사용되지 않던 Gemini 경로 코드를 정리하고 CPA 감사 표기와 버전 문서 설명도 함께 갱신했습니다.
- 릴리스 버전을 `0.1.18`로 올리고 workspace, 프런트엔드 패키지, Tauri 데스크톱, lock file, README, CHANGELOG의 버전 설명을 함께 갱신했습니다.

## [0.1.17] - 2026-04-05

### Added
- 请求日志新增“最终生效服务等级”口径，HTTP / WS 日志现在会同时保留客户端显式 `service_tier` 和请求改写后的最终值，方便核对平台 Key 默认 `Fast` 是否真正发往上游。
- 设置页新增全局“模型转发规则”，支持使用 `pattern=target` 形式做模型名改写，并在运行时请求改写阶段生效。

### Changed
- 普通平台 Key 的协议类型收敛为“通配兼容 (Codex / Claude Code)”，默认按请求路径自动选择 Claude 或 Codex / OpenAI 语义，减少重复维护多套 Key 的成本。
- 发布版本提升到 `0.1.17`，同步更新 workspace、前端包、Tauri 桌面端、锁文件、README 与 CHANGELOG 的版本说明。

## [0.1.16] - 2026-04-05

### Added
- 新增 `/v1/responses` WebSocket 请求支持，补齐传输类型识别、请求头归一化、代理运行时与请求日志链路。
- 账号页与用量弹窗新增附加额度窗口展示；刷新后会统一显示标准额度与 Code Review / Spark 等额外额度的剩余额度和重置时间。
- 网关 trace 新增 `CLIENT_SERVICE_TIER` 事件，记录 HTTP / WS 原始请求是否显式携带 `service_tier`、原始值以及日志归一化值，便于快速区分客户端显式 `fast` 与平台 Key 默认服务等级。

### Fixed
- 修复 HTTP 与 WS 请求日志中 `service_tier` 口径不一致的问题；现在仅当客户端请求自己显式携带 `service_tier` 时才记录 `fast`，不再把平台 Key 默认值误记成请求显式开启。
- 修复日志页服务等级展示与网关 on-wire 值不一致的问题；`priority` 会统一展示为 `fast`，未显式携带服务等级的请求继续显示为 `auto`。

### Changed
- 发布版本提升到 `0.1.16`，同步更新 workspace、前端包、Tauri 桌面端、锁文件、README 与 CHANGELOG 的版本说明。

## [0.1.15] - 2026-04-03

### Changed
- 发布版本提升到 `0.1.15`，同步更新 workspace、前端包、Tauri 桌面端、运行文档与 README 的版本说明。

## [0.1.14] - 2026-03-30

### Added
- 设置页新增“系统推导”按钮和“单账号并发上限”，可以按当前机器资源一键回填并立即生效。
- 入口层新增短队列等待与超载快速退化，避免高并发直接拖死服务进程。

### Changed
- README、workspace、前端包、Tauri 桌面端与版本一致性校验脚本统一提升到 `0.1.14`。

## [0.1.13] - 2026-03-25

### Added
- 新增“聚合 API”管理页，支持供应商名称、顺序优先级、按 `Codex / Claude` 分类、连通性测试与最小转发上游管理。
- 平台密钥新增 `账号轮转 / 聚合 API 轮转` 策略，聚合 API 轮转会按顺序优先命中对应供应商，再继续下一个渠道。

### Fixed
- 修复桌面端服务启动与页面切换时的自动恢复行为，避免关停后被切页重新拉起，也避免断连时仪表盘误清空数据。

### Changed
- README、workspace、前端包、Tauri 桌面端与版本一致性校验脚本统一提升到 `0.1.13`。

## [0.1.12] - 2026-03-20

### Fixed
- 修复平台密钥名称编辑链路在桌面端未完整透传的问题；现在 Web 与桌面端都能正确保存并回显名称，且支持中文名称。
- 修复平台密钥列表中密钥 ID 默认被截断的问题；现在会直接完整显示，便于核对与排查。

### Changed
- README 新增赞助支持入口与赞助区跳转，方便从文档顶部直接定位到赞助说明。
- 发布版本提升到 `0.1.12`，同步更新 workspace、前端包、Tauri 桌面端、版本一致性校验脚本与 README 最新版本说明。

## [0.1.11] - 2026-03-20

### Added
- 账号管理新增封禁识别、封禁筛选与“一键清理封禁账号”入口；`account_deactivated` 与 `workspace_deactivated` 会被自动识别为不可用信号，并可在列表中直接筛选和清理。
- 账号列表的 5 小时 / 7 天额度列现在会展示各自窗口的重置时间；仅返回 7 天窗口的 free 账号也会把重置时间显示到 7 天列。
- 平台密钥新增服务等级配置：`跟随请求`、`Fast`、`Flex`，其中 `Fast` 会映射为上游 `priority`，`Flex` 会直传为 `flex`。

### Fixed
- 修复桌面端平台密钥创建 / 编辑时 `serviceTier` 未透传导致“服务等级”保存后不生效、不回显的问题。
- 修复 Web 端在非首页刷新时偶发下载错误文件的问题，并修复部分运行环境下复制 API Key / 登录链接时 `navigator.clipboard.writeText` 不可用导致的复制失败。
- 修复设置页“检查更新”按钮在自动静默检查更新时持续错误转圈的问题；现在只有手动点击时才显示加载状态。

### Changed
- 网关主链路继续向 Codex-first 收口：会话绑定、自动切号即切线程、`originator` / `User-Agent` / 请求压缩等出站语义已进一步对齐，并移除了旧兼容路径遗留的 upstream cookie 链路。
- 设置页补回服务监听地址切换，可在 `localhost` 与 `0.0.0.0` 之间切换；README 与文档也已同步收口到当前主线路径。
- 发布版本提升到 `0.1.11`，同步更新 workspace、前端包、Tauri 桌面端、版本一致性校验脚本与 README 最新版本说明。

## [0.1.10] - 2026-03-18

### Fixed
- 修复 Web / Docker 版误走桌面专属命令分支、账号启用 / 禁用缺少 `sort` 参数导致无法切换状态，以及账号详情刷新失败后状态列不及时回刷的问题。
- 修复禁用账号仍参与手动批量刷新与后台用量轮询的问题；批量刷新与后台轮询现已跳过手动禁用账号，并按并发 worker 执行。
- 修复账号状态语义混乱问题：手动禁用统一为 `disabled`，额度用尽与 `usage endpoint 401` 统一为 `unavailable`，`refresh token 401` 相关链路也统一落成 `unavailable`，前端状态展示同步收口为“已禁用 / 不可用”。
- 修复 Windows 本地 Web 启动器关闭控制台窗口后 `codexmanager-service` / `codexmanager-web` 仍残留后台的问题；启动器现在会通过 Job Object 一并回收子进程。

### Changed
- 发布版本提升到 `0.1.10`，同步更新 workspace、Tauri 桌面端、前端包版本、README 最新版本说明和版本一致性测试。

## [0.1.9] - 2026-03-18

### Added
- 请求日志现在支持后端分页、后端统计、首尝试账号和尝试链路展示，便于区分实际命中账号与 failover 后的最终账号。
- 设置页新增 free / 7 天单窗口账号使用模型配置，free 类账号会统一按设置模型发起请求。

### Fixed
- 修复桌面端启动误判、`/rpc` 空响应、`spawn_blocking` 缺失导致的刷新失败、用量弹窗刷新不同步、首次切页卡顿、Hydration 不一致等稳定性问题。
- 修复 refresh token 误摘号、free 账号请求模型未正确改写、优先账号行为不稳定，以及 `503 no available account` 缺少上下文诊断的问题。
- 修复 release workflow 中 pnpm 版本与当前锁文件不匹配导致的 verify 失败问题。

### Changed
- 旧前端已移除，桌面端与 Web 管理界面统一收口到新的 `apps` 前端；账号管理、平台密钥、请求日志、设置页和导航布局都做了整轮桌面优先重构。
- Codex 请求链路继续按实际 on-wire 行为收口：登录 / callback / workspace 校验、refresh 语义、`/v1/responses` 与 `/v1/responses/compact` 重写、线程锚点、请求压缩、错误摘要和 fallback 诊断均已继续对齐。
- 网关失败诊断和磁盘日志继续收敛，compact 假成功体、HTML/challenge 页、`401 refresh` 子类和 exhausted 候选链路都会输出更明确的摘要。
- 统一将发版版本提升到 `0.1.9`，同步更新 workspace、Tauri 桌面端、`tauri.conf.json` 与前端包版本。
- GitHub Release workflow 中固定的 Tauri CLI 版本已对齐到当前 Rust 侧实际使用版本，减少打包阶段的 CLI / crate 漂移风险。
- 发布文档与 README 已同步更新到 `v0.1.9`，并修正前端静态导出目录说明为 `apps/out`。

## [0.1.8] - 2026-03-11

### Fixed
- Removed the default `https://api.openai.com/v1` fallback path for ChatGPT-backed requests; upstream `challenge` and `403` outcomes are now returned from the primary login-account path instead of being rewritten into local fallback errors.
- ChatGPT login-account requests now recover from `401` by refreshing the local `access_token` with the stored `refresh_token` and retrying the current request once.

### Changed
- ChatGPT login-account turns now use `access_token` directly on the primary upstream path and no longer mix in `api_key_access_token` semantics.
- Synthetic gateway terminal failures now return structured OpenAI-style `error.message / error.type / error.code` payloads while keeping the existing trace and error-code headers.

## [0.1.7] - 2026-03-11

### Added
- 设置页新增网关传输参数：支持直接配置上游流式超时与 SSE keepalive 间隔，并在 service 运行时热生效。
- 桌面端启动快照补齐：仪表盘统计、账号用量状态、请求日志首屏会优先恢复最近一次快照，减少源码运行或服务重启后的全 0 / 未知状态。

### Fixed
- 修复 `codexmanager-web` 的访问密码会话跨重启仍可继续使用的问题；关闭并重新打开 Web 进程后，旧登录 Cookie 会失效，需要重新验证密码。
- 修复源码运行 `codexmanager-web` 时的启动与根路由兼容问题，减少 Web 静态资源与根路径在 Axum 路由下的不一致行为。
- 修复长输出场景下的 SSE 空闲断流重连问题，降低长时流式响应被误判中断的概率。
- 修复设置页保存上游代理、平台密钥创建弹窗关闭与重复提交、登录成功后账号表格未刷新等桌面交互问题。
- 修复模型拉取默认附加版本参数导致的部分上游兼容性问题，模型请求改为默认不附带版本号。
- 修复账号导入与登录回调两条链路的账号归并逻辑不一致问题，统一按同一身份规则新增或更新账号。
- 修复 Claude / Anthropic `/v1/messages` 适配在多 MCP server 场景下的工具截断问题；不再因前 16 个工具占满而丢失后续 server 的工具。
- 修复 Claude / Anthropic `/v1/messages` 链路缺少长工具名缩短与响应还原的问题，避免 MCP 工具名过长时映射不稳定。

### Changed
- 网关失败响应增加结构化 `errorCode` / `errorDetail` 字段，并同步补充 `X-CodexManager-Error-Code`、`X-CodexManager-Trace-Id` 响应头，便于客户端与日志系统追踪失败链路。
- 协议适配继续对齐 Codex / OpenAI 兼容生态：进一步统一 `/v1/chat/completions`、`/v1/responses`、Claude `/v1/messages` 的转发语义，并稳固 `tools` / `tool_calls`、thinking / reasoning、流式桥接和响应还原链路。
- 设置页与运行时配置继续收敛：背景任务、网关传输、上游代理、Web 安全等高频配置统一由 `app_settings` 持久化并回填到当前进程。
- 桌面与 service 启动链路继续治理，收敛 Web / service / desktop 之间的启动边界与启动顺序，减少源码运行与打包运行的行为分叉。
- 项目内部继续推进长期维护向的重构治理：前端主入口、设置页、请求日志视图、Tauri 命令注册、service 生命周期、gateway protocol adapter、HTTP bridge、upstream attempt flow 等区域已进一步拆分模块边界，减少大文件与根层门面耦合。
- service / gateway 目录结构继续收敛，更多通配导入、跨层直连和超长门面清单已被显式依赖与分层模块替代，后续维护和协议回归定位成本更低。
- 发布链路继续收敛到 `release-all.yml` 单入口，并复用前端构建产物与协议回归基线，减少重复构建与发布时的协议回归风险。

## [0.1.6] - 2026-03-07

### Fixed
- 修复 `release-all.yml` 在手动关闭 `run_verify` 时仍强依赖预构建前端工件的问题；各平台任务缺少 `codexmanager-frontend-dist` 时会自动回退到本地 `pnpm install + build`。

### Changed
- Windows 桌面端发布产物继续收敛，仅保留 `CodexManager-portable.exe` 便携版，不再额外生成 `CodexManager-windows-portable.zip`。
- 完善 SOCKS5 上游代理支持与归一化，并补充设置页中的代理协议提示文案。

## [0.1.5] - 2026-03-06

### Added
- 新增“按文件夹导入”：桌面端可直接选择目录，递归扫描其中 `.json` 文件并批量导入账号。
- 新增 OpenAI 上游代理配置与请求头收敛策略开关，可在设置页直接保存并即时生效。
- 补充 chat tools 命中探针脚本，便于本地验证工具调用是否真正命中与透传。

### Fixed
- 修复 `tool_calls` / `tools` 相关回归：补齐 chat 聚合路径中的工具调用保留、工具名缩短与响应还原链路，避免工具调用在 OpenAI 兼容返回、流式增量和适配转换中丢失或名称错乱。
- 完善 OpenClaw / Anthropic 兼容返回适配，确保工具调用、SSE 增量和非流式 JSON 响应都能按兼容格式正确还原。
- 请求日志追踪增强，补充原始路径、适配路径和更多上下文，便于定位 `/v1/chat/completions -> /v1/responses` 转发与协议适配问题。

### Changed
- 网关协议适配进一步对齐 Codex CLI：`/v1/chat/completions` 与 `/v1/responses` 两条链路统一收敛到 Codex `responses` 语义，上游流式/非流式行为与官方更接近，兼容 Cherry Studio 等客户端的 OpenAI 兼容调用。
- 设置页顶部常用配置改为统一的三列行布局，代理配置与其保持一致；同时支持关闭窗口后隐藏到系统托盘运行。
- 发布流程整合为单一一键多平台 workflow，并收敛桌面端产物形态；Windows 直接提供 portable exe，macOS 统一使用 DMG 分发。

## [0.1.4] - 2026-03-03

### Added
- 新增“一键移除不可用 Free 账号”：批量清理“不可用 + free 计划”账号，并返回扫描/跳过/删除统计。
- 新增“导出用户”：支持选择本地目录并按“一个账号一个 JSON 文件”导出。
- 导入兼容增强：支持 `tokens.*`、顶层 `*_token`、camelCase 字段（如 `accessToken` / `idToken` / `refreshToken`）自动识别。

### Fixed
- 兼容旧 service：前端导入前会自动归一化顶层 token 格式，避免旧版后端报 `missing field: tokens`。

### Changed
- 账号管理页操作区整合为单一“账号操作”下拉菜单，替代右侧多按钮堆叠，界面更简洁。

[Unreleased]: https://github.com/qxcnm/Codex-Manager/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/qxcnm/Codex-Manager/compare/v0.4.4...v0.5.0
[0.4.4]: https://github.com/qxcnm/Codex-Manager/compare/v0.4.3...v0.4.4
[0.4.3]: https://github.com/qxcnm/Codex-Manager/compare/v0.4.2...v0.4.3
[0.4.2]: https://github.com/qxcnm/Codex-Manager/compare/v0.4.1...v0.4.2
[0.4.1]: https://github.com/qxcnm/Codex-Manager/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/qxcnm/Codex-Manager/compare/v0.3.9...v0.4.0
[0.3.9]: https://github.com/qxcnm/Codex-Manager/compare/v0.3.8...v0.3.9
[0.3.8]: https://github.com/qxcnm/Codex-Manager/compare/v0.3.7...v0.3.8
[0.3.7]: https://github.com/qxcnm/Codex-Manager/compare/v0.3.6...v0.3.7
[0.3.6]: https://github.com/qxcnm/Codex-Manager/compare/v0.3.5...v0.3.6
[0.3.5]: https://github.com/qxcnm/Codex-Manager/compare/v0.3.4...v0.3.5
[0.2.6]: https://github.com/qxcnm/Codex-Manager/compare/v0.2.3...v0.2.6
[0.1.19]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.19
[0.1.17]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.17
[0.1.16]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.16
[0.1.15]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.15
[0.1.14]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.14
[0.1.13]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.13
[0.1.12]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.12
[0.1.11]: https://github.com/qxcnm/Codex-Manager/compare/v0.1.10...v0.1.11
[0.1.10]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.10
[0.1.9]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.9
[0.1.8]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.8
[0.1.7]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.7
[0.1.6]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.6
[0.1.5]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.5
[0.1.4]: https://github.com/qxcnm/Codex-Manager/releases/tag/v0.1.4
